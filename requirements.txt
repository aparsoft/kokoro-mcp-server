# Aparsoft TTS - Streamlit Cloud Requirements
# Use this file for deploying to Streamlit Cloud

# Core TTS dependencies
kokoro>=0.9.2
soundfile>=0.12.1
librosa>=0.10.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=24.0.0
python-json-logger>=2.0.7

# Streamlit dependencies
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0
pyarrow>=14.0.0

# Optional: MCP support (comment out if not needed)
# fastmcp>=2.0.0
# mcp>=1.2.0

# Optional: CLI support (comment out if not needed)
# typer>=0.9.0
# rich>=13.0.0
